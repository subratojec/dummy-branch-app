# .env.dev
ENV=dev
FLASK_ENV=development
LOG_LEVEL=debug

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=microloans
DB_PORT=5432
DB_INTERNAL_PORT=5432

API_PORT=8000

# dev-only: enable bind mounts (hot reload)
DEV_MOUNT=true
    
# Optional explicit DB url (dev can use direct postgres defaults)
DATABASE_URL=postgresql://postgres:postgres@db:5432/microloans
